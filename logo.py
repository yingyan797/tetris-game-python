from ast import Str
from operate import Structure

class Canvas:
    def __init__(self, r, c):
        self.struct = Structure(r,c)
    def action(num, op):
        match op:
            case "pu":
            case "pd":
            case "save":
            case "undo":
            case "pu":
            case "pu":
    
